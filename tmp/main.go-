package main

import (
	"log"
	"os/exec"
	"strings"
)

func main() {
	cmd := exec.Command("./foo.py")
	cmd.Stdin = strings.NewReader("abcdefghijklmn")
	err := cmd.Run()
	if err != nil {
		log.Fatal(err)
	}
}
